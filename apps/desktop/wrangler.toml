name = "wawa-smart-erp"
pages_build_output_dir = "dist/renderer"
compatibility_date = "2024-01-01"

# 주의: 시크릿/API 키는 본 파일에 절대 쓰지 말 것 (public github repo 노출).
# - frontend (Vite SPA) 는 어떤 API 키도 필요하지 않음
# - workers/ 백엔드의 GEMINI_API_KEY 등은 Cloudflare worker secret 으로 관리
#   설정: cd workers && wrangler secret put GEMINI_API_KEY
